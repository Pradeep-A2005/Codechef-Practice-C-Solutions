#include <stdio.h>

int main(void) {
	int t,a,b,c;
	scanf("%d",&t);
	for(int i=0;i<t;i++)
	{
	    scanf("%d%d%d",&a,&b,&c);
	    if(a>=10 && b>=10 && c>=10 && a+b+c>=100)
	    printf("PASS\n");
	    else
	    printf("FAIL\n");
	}
}


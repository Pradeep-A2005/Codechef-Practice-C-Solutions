#include<stdio.h>
int main()
{
    int n,k,num,count=0;
    scanf("%d%d",&n,&k);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&num);
        if(num%k==0)
        count++;
    }
    printf("%d",count);
}

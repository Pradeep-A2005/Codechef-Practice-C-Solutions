#include <stdio.h>
int main(void) {
	int t;
	scanf("%d",&t);
	int n,x;
	for(int i=0;i<t;i++)
	{
	    int c=0;
	    scanf("%d%d",&n,&x);
	    while(n>x)
	    {
	        x+=4;
	        c++;
	    }
	    printf("%d\n",c);
	}

}


#include<stdio.h>
#include<math.h>
int main()
{
    int t;
    scanf("%d",&t);
    int n,x;
    for(int i=0;i<t;i++)
    {
        scanf("%d%d",&n,&x);
        int num=n*x;
        int dig=(int)log10(num)+1;
        (dig==5)?printf("yes\n"):printf("no\n");
    }
}

#include <stdio.h>
int main(void) {
	int t,w,x,y,z;
	scanf("%d",&t);
	for(int i=0;i<t;i++)
	{
	    scanf("%d%d%d%d",&w,&x,&y,&z);
	    if(w+(y*z)==x)
	    printf("filled\n");
	    else if(w+(y*z)>=x)
	    printf("overflow\n");
	    else if(w+(y*z)<=x)
	    printf("Unfilled\n");
	    
	}

}


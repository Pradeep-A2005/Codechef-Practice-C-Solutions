#include <stdio.h>
int main(void) {
	int t,x,y,z;
	scanf("%d",&t);
    for(int i=0;i<t;i++)
    {
        scanf("%d%d%d",&x,&y,&z);
        int k=x*y;
        int l=z*24*60;
        (k<=l)?printf("Yes\n"):printf("No\n");
    }
}


#include <stdio.h>
int main(void) {
	int t;
    scanf("%d",&t);
    for(int i=0;i<t;i++){
        int a,b;
        scanf("%d%d",&a,&b);
        int d=a-b;
        if(d>b)
        printf("%d\n",b);
        else
        printf("%d\n",d);
    }
}


#include <stdio.h>
int main(void) {
	int t,a,b;
	scanf("%d",&t);
	for(int i=0;i<t;i++)
	{
	    scanf("%d%d",&a,&b);
	    int x=a*100;
	    int y=b*10;
	    if(x>=y)
	    printf("Cloth\n");
	    else
	    printf("Disposable\n");
	}

}


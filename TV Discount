#include <stdio.h>
int main(void) {
	int t,a,b,c,d;
	scanf("%d",&t);
	for(int i=0;i<t;i++)
	{
	    scanf("%d%d%d%d",&a,&b,&c,&d);
	    if((a-c)>(b-d))
	    printf("Second\n");
	    if((a-c)<(b-d))
	    printf("First\n");
	    if((a-c)==(b-d))
	    printf("Any\n");
	}
}


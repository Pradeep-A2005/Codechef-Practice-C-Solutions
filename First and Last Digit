#include <stdio.h>
#include<math.h>
int main(void) {
	int t,n;
	scanf("%d",&t);
	for(int i=0;i<t;i++)
	{
	    scanf("%d",&n);
	    int dig=(int)log10(n);
	    int first=n/(int)pow(10,dig);
	    int last=n%10;
	    int sum=first+last;
	    printf("%d\n",sum);
	}
}

